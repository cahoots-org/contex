apiVersion: v1
kind: Service
metadata:
  name: {{ include "contex.fullname" . }}
  labels:
    {{- include "contex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contex.selectorLabels" . | nindent 4 }}
