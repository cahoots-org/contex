apiVersion: apps/v1
kind: Deployment
metadata:
  name: contex
spec:
  template:
    spec:
      containers:
      - name: contex
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
