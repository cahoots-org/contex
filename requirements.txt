# Production Dependencies Only
# For development/testing, use requirements-dev.txt

# Core dependencies
redis==5.0.1
pydantic==2.5.0
fastapi==0.104.0
uvicorn[standard]==0.24.0
httpx==0.25.2

# CPU-only PyTorch (must be installed before sentence-transformers)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# Embedding model for semantic matching
sentence-transformers==3.3.1
scikit-learn==1.3.2

# Utilities
python-dotenv==1.0.0
tiktoken==0.5.2
