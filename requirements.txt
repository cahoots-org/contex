# Context Engine Service Dependencies
# This is for the standalone context-engine microservice deployment (CPU-ONLY)

# Core dependencies
redis==5.0.1
pydantic==2.5.0
fastapi==0.104.0
uvicorn[standard]==0.24.0
httpx==0.25.2

# Embedding model for semantic matching (v2)
sentence-transformers==3.3.1
scikit-learn==1.3.2  # For cosine similarity

# Utilities
python-dotenv==1.0.0
tiktoken==0.12.0  # Token counting for context size limits

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
fakeredis==2.20.1
